id: no-byte-pointer-arithmetic
language: C
severity: hint
message: Prefer struct field access over byte-pointer offset arithmetic.
note: "Byte-cast offset expressions usually indicate a missing struct field. Add a typed field when the layout is known."
rule:
  pattern: '(u8 *)$BASE + $OFFSET'
