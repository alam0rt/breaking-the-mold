id: no-raw-address-symbols
language: C
severity: hint
message: Rename raw address symbols to meaningful C identifiers with asm labels.
note: "Prefer a meaningful C identifier with an asm label that preserves the raw D_800xxxxx linker symbol."
rule:
  kind: identifier
  regex: '^D_[0-9A-Fa-f]{8}$'
